<Window x:Class="RestaurantManagement.Views.EmployeesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Employee Management" Height="550" Width="1000"
        Background="#F4F7F9" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="BorderBrush" Value="#DCDFE6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Opacity" Value="0.8"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowBackground" Value="White"/>
            <Setter Property="AlternatingRowBackground" Value="#F9FAFB"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="CanUserAddRows" Value="False"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#2C3E50" Padding="20,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="👥" FontSize="24" Margin="0,0,15,0"/>
                <TextBlock Text="EMPLOYEE MANAGEMENT" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,10,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#D0D7DE" Opacity="0.2" ShadowDepth="2"/>
                </Border.Effect>

                <DockPanel Margin="15">
                    <TextBlock Text="Staff List" DockPanel.Dock="Top" FontSize="18" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,15"/>

                    <Button Content="🗑️ Dismiss Selected Employee" 
                            DockPanel.Dock="Bottom" 
                            Click="DeleteEmployee_Click" 
                            Style="{StaticResource ModernButtonStyle}" 
                            Background="#E74C3C" 
                            Margin="0,15,0,0" Height="40"/>

                    <DataGrid x:Name="EmployeesDataGrid" 
                              ItemsSource="{Binding Employees}" 
                              SelectedItem="{Binding SelectedEmployee}"
                              AutoGenerateColumns="False" 
                              IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" Width="*"/>
                            <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" Width="*"/>
                            <DataGridTextColumn Header="Position" Binding="{Binding EmployeeType}" Width="*"/>
                            <DataGridTextColumn Header="Salary" Binding="{Binding Salary, StringFormat={}{0:C}}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Background="White" CornerRadius="12" Margin="10,0,0,0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="15" Color="#D0D7DE" Opacity="0.2" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel Margin="20">
                    <TextBlock Text="Hire New Staff" FontSize="18" FontWeight="Bold" Foreground="#34495E" Margin="0,0,0,20"/>

                    <TextBlock Text="First Name" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                    <TextBox x:Name="FirstNameTxt"/>

                    <TextBlock Text="Last Name" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                    <TextBox x:Name="LastNameTxt"/>

                    <TextBlock Text="Position" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                    <ComboBox x:Name="PositionComboBox" />

                    <TextBlock Text="Salary" Foreground="#7F8C8D" Margin="0,0,0,5"/>
                    <TextBox x:Name="SalaryTxt"/>

                    <Button Content="➕ Add to Staff" 
                            Click="AddEmployee_Click" 
                            Style="{StaticResource ModernButtonStyle}" 
                            Background="#2ECC71" 
                            Height="45" Margin="0,10,0,0"/>

                    <TextBlock Text="* Check data before submitting" 
                               FontSize="11" Foreground="#95A5A6" FontStyle="Italic" 
                               HorizontalAlignment="Center" Margin="0,15,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>